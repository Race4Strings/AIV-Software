# Sami Handoff — March 5, 2026

## Branch
**`mvp/vault`** on both repos. Pull this branch.

## Backend API
`https://aiv-backend-mvp.up.railway.app`

## Demo Credentials
- Email: `demo@vault.dev`
- Password: `VaultDemo#2026`
- This account exists and can sign in, but has NOT gone through onboarding (no twin created)

---

## CRITICAL: Everything Must Work End-to-End

Sami, you need to click through EVERY page and make sure it works. Not just "loads" — actually works. Data saves, data loads, no console errors, no broken states. Here's the full checklist:

---

## 1. Auth Flow (MUST WORK)

### Sign Up
- `POST /auth/signup` — creates account
- Frontend: `/auth/signup`
- After signup, user should be redirected to email verification

### Sign In
- `POST /auth/signin` with `{ identifier, password }`
- Frontend: `/auth/signin`
- Must set session cookie and store user in localStorage
- After signin, redirect to `/` (vault dashboard)

### Forgot Password
- `POST /auth/forgot-password` with `{ email }`
- Frontend: `/auth/forgot-password`
- Must generate a reset token and send email (or store token for manual testing)

### Reset Password (NEW PAGE)
- `POST /auth/reset-password` with `{ token, new_password }`
- Frontend: `/auth/reset-password?token=xxx`
- Validates token, updates password
- **Backend endpoint already exists** at `/auth/reset-password` — just make sure it works with the frontend's payload format

### Sign Out
- `POST /auth/signout`
- Clears session, clears localStorage, redirects to `/auth/signin`

---

## 2. Onboarding (MUST WORK)

- Frontend: `/onboard`
- This creates the user's digital twin (clone)
- Uses these backend endpoints:
  - `POST /clone` — creates the clone
  - `PUT /clone/{id}/personality` — saves personality data
  - `PUT /clone/{id}/visual` — saves visual identity
  - `PUT /clone/{id}/voice` — saves voice data
  - `PUT /clone/{id}/rights` — saves governance/rights
  - `PUT /clone/{id}/knowledge` — saves knowledge base
  - `POST /clone/{id}/activate` — activates the clone
- After onboarding completes, user should land on the vault dashboard with their twin data populated

**TEST**: Create a fresh account, go through full onboarding, verify all data persists. Voice cloning must trigger during onboarding — if it doesn't, reference the `manish/dev-hold` branch which had this working. Extract what you need from there.

---

## 3. Vault Dashboard (MUST WORK)

- Frontend: `/` (when logged in)
- Loads twin data, certification status, documents, audit logs
- Shows Protection Status cards (certification, doc count, protection level)
- Shows Protection Checklist
- Shows Asset Overview grid
- Shows Recent Activity feed

**Frontend API calls:**
- `GET /twins` → list twins (note: frontend uses `/twins`, backend uses `/clone`)
- `GET /twins/{id}` → get full twin
- `GET /twins/{id}/completeness` → completeness score
- `GET /twins/{id}/certifications/latest` → latest cert
- `GET /twins/{id}/documents` → document list
- `GET /twins/{id}/audit-logs` → activity feed

**IMPORTANT**: The frontend API client calls `/twins/...` but the backend routes are under `/clone/...`. Either:
1. Add `/twins/` aliases in the backend that map to the clone endpoints, OR
2. Update the frontend API client to use `/clone/` paths

This mismatch is likely why data doesn't load properly. **Fix this.**

---

## 4. Identity Profile (MUST WORK)

- Frontend: `/twin`
- 6 tabs: Identity, Personality, Visual, Voice, Commercial, Governance
- Each tab loads data from the twin and allows inline editing
- Edits save via PUT to the respective clone endpoints

---

## 5. Documents (MUST WORK)

### Document Library
- Frontend: `/twin/documents`
- CRUD operations: create, list, edit, delete
- **Backend needed**: Full CRUD at `/twins/{id}/documents` (or `/clone/{id}/documents`)
  - `GET /twins/{id}/documents` — list all docs
  - `POST /twins/{id}/documents` — create doc with `{ title, doc_type, content, status }`
  - `GET /twins/{id}/documents/{doc_id}` — get single doc
  - `PUT /twins/{id}/documents/{doc_id}` — update doc
  - `DELETE /twins/{id}/documents/{doc_id}` — delete doc

### Legal Templates
- Frontend: `/twin/documents/templates`
- 4 templates: DMCA Takedown, Cease & Desist, AI Usage Consent, Right of Publicity
- Templates auto-populate fields from twin data
- Generated documents are markdown — need to be saved via the documents CRUD above

---

## 6. Certification (MUST WORK)

- Frontend: `/twin/certification`
- Create certification: `POST /twins/{id}/certifications` — generates SHA-256 hash of twin data
- List certs: `GET /twins/{id}/certifications`
- Latest cert: `GET /twins/{id}/certifications/latest`
- Public verification: `GET /verify/{hash}` (already exists in backend)
- PDF export works client-side (browser print) — no backend needed

---

## 7. Voice (MUST WORK)

- Frontend: `/twin/voice`
- Upload voice samples, check cloning status
- Uses `/upload/voice` and `/clone/{id}/voice`

---

## 8. Deals (MUST WORK)

- Frontend: `/deals`
- List and manage deals
- Whatever endpoints exist for deals, make sure they work

---

## 9. Settings

- Frontend: `/settings`
- Shows account info, theme toggle, sign out
- Accessible from profile avatar dropdown (top right)
- No backend work needed

---

## 10. Talk to AIV (Chat)

- Frontend: `/aiv`
- Chat interface with AI
- Uses workspaces and chats endpoints
- Lower priority but should at least not crash

---

## Known Issues

1. **Frontend uses `/twins/` but backend uses `/clone/`** — this is the #1 thing to fix. Either add aliases or update one side.
2. **Demo account has no twin** — create a fresh account and go through full onboarding to test everything.
3. **Audit logs endpoint** — frontend expects `GET /twins/{id}/audit-logs`. Make sure this exists.
4. **Completeness endpoint** — frontend expects `GET /twins/{id}/completeness`. Make sure this exists.
5. **Voice cloning during onboarding** — onboarding should trigger voice cloning. If it doesn't work, reference the `manish/dev-hold` branch which had voice cloning working properly. Extract what you need from there.

---

## Manual QA Checklist (DO ALL OF THESE)

- [ ] Sign up with a new email
- [ ] Verify email
- [ ] Sign in
- [ ] Complete full onboarding (all steps)
- [ ] Land on vault dashboard — all cards populated
- [ ] Click into Identity → edit a field → save → verify it persists
- [ ] Go to Documents → create a document → edit it → delete it
- [ ] Go to Templates → generate a DMCA takedown → verify it saves
- [ ] Go to Certification → certify identity → verify hash appears
- [ ] Open public verification URL → verify it loads
- [ ] Go to Voice → upload a sample (or verify status)
- [ ] Go to Deals → verify page loads
- [ ] Go to Settings → toggle theme → sign out
- [ ] Sign back in → verify all data still there
- [ ] Forgot password → reset password → sign in with new password
- [ ] Check browser console — ZERO errors on every page
- [ ] Test on mobile viewport (responsive)

---

## Environment Setup
- Backend: Railway project `peaceful-curiosity`, mvp environment
- Frontend: Vercel (auto-deploys from `mvp/vault`)
- Database: PostgreSQL on Railway
- Backend API: `https://aiv-backend-mvp.up.railway.app`
