# Sami Handoff — March 6, 2026 (Final Update)

## Branch
**`mvp/vault`** on both repos. Pull this branch.

## Backend API
`https://aiv-backend-mvp.up.railway.app`

## Demo Credentials
- Email: `demo@vault.dev` / Password: `VaultDemo#2026`
- This account has a Jennifer Aniston twin already. For testing onboarding, create a fresh account.

---

## What Was Fixed Today (Ricardo — Day 6)

### Critical Backend Fix: JSON Mutation Tracking
SQLAlchemy wasn't persisting onboarding step data (steps 2-6 were being lost). Fixed by creating new dict/list copies before reassigning JSON columns. Also fixed bio extraction — was pulling from step 1 instead of step 3. **Deployed to Railway.**

### Protection Banner on Identity Profile
Added a visible "Identity Certified & Protected" banner (green shield) or "Certification Pending" (amber shield) to the twin profile header. Shows on every identity page visit.

### API-Verified End-to-End Flow
Tested the full pipeline via API calls:
- Signup → returns dev_otp ✅
- Verify email → sets session cookie ✅
- Start onboarding → creates session ✅
- Submit steps 1-6 → all data persists correctly ✅
- Trigger research → Gemini AI returns ALCM data ✅
- Complete onboarding → creates twin with ALCM + bio ✅
- Certification → SHA-256 hash generated ✅
- Public verification → returns cert metadata ✅

---

## Architecture Overview

The frontend uses a Next.js rewrite proxy at `/api/backend/*` that forwards to the Railway backend. No CORS issues. The backend uses `/twins/` prefix. All major endpoints exist and match what the frontend expects.

---

## What You Need To Do

### Priority 1: Full Visual QA
Click through EVERY page in the browser. Not "it loads" but "it actually works." Data saves, data loads, no console errors, no toast errors.

### Priority 2: Voice Cloning During Onboarding
The onboarding flow triggers voice cloning via ElevenLabs. The endpoint `POST /onboarding/{session_id}/voice` exists and is wired. Make sure:
- Video recording works in browser (camera + mic permissions)
- Voice sample uploads correctly
- ffmpeg extracts audio on Railway (installed in Dockerfile)
- ElevenLabs cloning triggers and completes
- Twin's `voice_status` updates to "ready"
- If it doesn't work, reference the `manish/dev-hold` branch

### Priority 3: Password Reset Flow
Verify forgot-password and reset-password work end-to-end. The pages exist, the backend endpoints exist.

### Priority 4: Deal Creation Payload
The frontend sends `{ twin_id, data: {...} }` but the backend expects flat fields. Fix the mismatch.

---

## Existing Backend Endpoints (All Implemented)

### Auth (`/auth`)
- POST /auth/signup, /auth/signin, /auth/signout
- POST /auth/verify-email, /auth/resend-otp
- POST /auth/forgot-password, /auth/reset-password
- GET /auth/me

### Twins (`/twins`)
- POST /twins, GET /twins, GET /twins/{id}
- PUT /twins/{id}, PATCH /twins/{id}, DELETE /twins/{id}
- GET /twins/{id}/completeness
- POST /twins/{id}/voice/tts (text-to-speech demo)

### Documents (`/twins/{id}/documents`)
- Full CRUD

### Certifications (`/twins/{id}/certification`)
- POST (create), GET (list), GET /latest

### Onboarding (`/onboarding`)
- POST /start, GET /{id}, PUT /{id}/step, POST /{id}/complete
- POST /{id}/voice, POST /{id}/research, GET /{id}/research

### Public
- GET /verify/{hash} (no auth required)

---

## Manual QA Checklist

- [ ] Sign up with a new email
- [ ] Verify email (DEV_MODE=true, OTP shows in response)
- [ ] Complete full onboarding (all 6 steps including video)
- [ ] Research populates ALCM data
- [ ] Voice cloning triggers and completes
- [ ] Dashboard loads with protection status cards
- [ ] Identity profile shows all ALCM data with protection banner
- [ ] Edit a field, save, verify it persists
- [ ] Generate a legal document from templates
- [ ] Certify identity, verify hash appears
- [ ] Public verification URL works
- [ ] Voice TTS demo works (if voice is cloned)
- [ ] Browser console: ZERO errors on every page
- [ ] No toast error notifications anywhere

---

## Environment
- Railway project: `peaceful-curiosity`, mvp environment
- Backend service: `AIV-Backend`
- DEV_MODE=true (OTP shows in API response, no real email)
- ElevenLabs API key configured
- Gemini AI key configured for research agent
- CORS whitelist: localhost:3000-3005 + production domains
